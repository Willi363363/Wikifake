# Generated or out of scope
pnpm-lock.yaml
node_modules/
coverage/
dist/
.turbo/

# Documentation has its own formatting rules (plans/method/).
plans/
*.md

# Drizzle owns these: it writes the SQL and reads its own snapshots back to
# compute the next diff. Reformatting a snapshot is how a migration chain starts
# disagreeing with the database it describes.
packages/db/migrations

# Frozen Wikipedia HTML. The fixtures claim to be byte-for-byte what MediaWiki
# served, and a test asserts on the whitespace they carry — reformatting them
# would make both false, and would silently change the text the parity test
# reads.
packages/article/fixtures
